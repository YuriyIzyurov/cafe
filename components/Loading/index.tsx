import React from 'react';
import {LoadingContainer} from "./LoadingStyles";


const Loading = () => {
    return (
        <LoadingContainer>
            Loading...
        </LoadingContainer>
    );
};

export default Loading;